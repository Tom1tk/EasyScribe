# EasyScribe - Python dependencies

# Speech-to-text engine (uses ctranslate2 under the hood)
faster-whisper>=1.0.3

# Speaker diarization engine (ONNX Runtime — supports CPU and CUDA via 'provider')
# CUDA builds use the '+cuda' wheel variant — see build_windows.bat.
sherpa-onnx>=1.10.0

# GUI framework
customtkinter>=5.2.2

# Drag-and-drop support for Tkinter
tkinterdnd2>=0.3.0

# Portable executable builder
pyinstaller>=6.10.0
