# Transcriber7 - Python dependencies
#
# IMPORTANT: Before running pip install, choose one of the torch lines below:
#
#   CPU only (smaller download, slower inference):
#     torch>=2.3.0
#
#   NVIDIA GPU with CUDA 11.8 (recommended for speed):
#     torch>=2.3.0+cu118
#     Install via: pip install torch>=2.3.0 --index-url https://download.pytorch.org/whl/cu118
#
#   NVIDIA GPU with CUDA 12.1:
#     torch>=2.3.0+cu121
#     Install via: pip install torch>=2.3.0 --index-url https://download.pytorch.org/whl/cu121
#
# The build script (build_windows.bat) installs the CPU version by default.
# Edit the script to switch to a CUDA build if your target machines have NVIDIA GPUs.

# Speech-to-text engine (uses ctranslate2 under the hood)
faster-whisper>=1.0.3

# GUI framework
customtkinter>=5.2.2

# Drag-and-drop support for Tkinter
tkinterdnd2>=0.3.0

# Portable executable builder
pyinstaller>=6.10.0

# PyTorch (CPU version — see notes above to switch to CUDA)
# Installed separately in build_windows.bat to allow index-url override
# torch>=2.3.0
